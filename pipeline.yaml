version: 1

prompts_dir: prompts/
state_dir: logs/
reports_dir: reports/

repos:
  textworkspace: .

defaults:
  model: sonnet
  budget_usd: 1.50
  max_turns: 100
  profile: personal
  claude_cmd: claude-personal

max_parallel: 1
total_budget_usd: 25.00
warn_at_pct: 80
