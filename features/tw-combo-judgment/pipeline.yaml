version: 1

prompts_dir: prompts/
state_dir: logs/
reports_dir: reports/

repos:
  textworkspace: ../..

defaults:
  model: opus
  budget_usd: 3.00
  max_turns: 150

max_parallel: 1
total_budget_usd: 30.00
warn_at_pct: 80
